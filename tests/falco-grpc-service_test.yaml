suite: falco grpc service creation per cluster
release:
  name: falco
templates:
- charts/falco/templates/grpc-service.yaml
values:
- ../values-subchart-overrides.yaml
tests:
- it: omits the grpc service since grpc is disabled for all clusters
  asserts:
  - hasDocuments:
      count: 0
